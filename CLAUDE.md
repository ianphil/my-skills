# CLAUDE.md



## Repository

Collection of Claude Code skills. Each skill lives in `skills/<name>/SKILL.md` with optional `scripts/` directory.

Plugin manifest: `.claude-plugin/marketplace.json`

## Creating Skills

Use `/skill-creator` to create new skills in this repo. It provides the proper SKILL.md format with YAML frontmatter and structure.
