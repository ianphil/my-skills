# AGENTS.md



## Repository

Collection of AI coding agent skills. Each skill lives in `<name>/SKILL.md` at the repo root with optional `scripts/` directory.

## Creating Skills

Use `/skill-creator` to create new skills in this repo. It provides the proper SKILL.md format with YAML frontmatter and structure.
